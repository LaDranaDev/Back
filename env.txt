apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  namespace: mx-h2h-bo-monitoreo-dev
  name: ''
spec:
  selector: {}
  replicas: 3
  template:
    metadata:
      labels: {}
    spec:
      containers:
        - name: container
          image: >-
            image-registry.openshift-image-registry.svc:5000/openshift/httpd:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          env: []
      imagePullSecrets: []
  strategy:
    type: Rolling
    rollingParams:
      timeoutSeconds: 600
      updatePeriodSeconds: 1
      intervalSeconds: 1
      maxSurge: 25%
      maxUnavailable: 25%
  triggers:
    - type: ConfigChange
  paused: false




DeploymentConfig Completo con Variables de Entorno
Código 
¡Listo! Ahí tienes tu DeploymentConfig completo con las variables de entorno agregadas.

Solo copia y pega exactamente ese YAML en tu editor de OpenShift y haz clic en "Crear".

Las variables de entorno que agregué:

✅ H2H_DB_URL - para conectarse a dbtredlo21mxg01.ilm.dev.mx.corp:1660/deatih2h
✅ H2H_DB_USERNAME - usará deatih2h
✅ H2H_DB_PASSWORD - usará deatih2h
✅ Variables de logging - para ver las conexiones en los logs del pod
Una vez que






apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  namespace: mx-h2h-bo-monitoreo-dev
  name: ''
spec:
  selector: {}
  replicas: 3
  template:
    metadata:
      labels: {}
    spec:
      containers:
        - name: container
          image: >-
            image-registry.openshift-image-registry.svc:5000/openshift/httpd:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          env:
          - name: H2H_DB_DRIVER
            valueFrom:
              configMapKeyRef:
                name: h2h-database-config
                key: H2H_DB_DRIVER
          - name: H2H_DB_URL
            valueFrom:
              configMapKeyRef:
                name: h2h-database-config
                key: H2H_DB_URL
          - name: H2H_DB_USERNAME
            valueFrom:
              configMapKeyRef:
                name: h2h-database-config
                key: H2H_DB_USERNAME
          - name: H2H_DB_PASSWORD
            valueFrom:
              configMapKeyRef:
                name: h2h-database-config
                key: H2H_DB_PASSWORD
          - name: H2H_DB_POOL_SIZE
            valueFrom:
              configMapKeyRef:
                name: h2h-database-config
                key: H2H_DB_POOL_SIZE
          - name: H2H_DB_CONNECTION_TIMEOUT
            valueFrom:
              configMapKeyRef:
                name: h2h-database-config
                key: H2H_DB_CONNECTION_TIMEOUT
          - name: LOGGING_LEVEL_COM_ZAXXER_HIKARI
            value: "DEBUG"
          - name: LOGGING_LEVEL_ORG_HIBERNATE_SQL
            value: "DEBUG"
          - name: SPRING_JPA_SHOW_SQL
            value: "true"
      imagePullSecrets: []
  strategy:
    type: Rolling
    rollingParams:
      timeoutSeconds: 600
      updatePeriodSeconds: 1
      intervalSeconds: 1
      maxSurge: 25%
      maxUnavailable: 25%
  triggers:
    - type: ConfigChange
  paused: false
